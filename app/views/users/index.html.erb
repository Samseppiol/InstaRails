<h1 align='center'>Insta Rails Users</h1>
<div class='row wrapper'>
  <div class=' col-md-8 col-md-offset-2'>
    <div align='center'>
      <% @users.each do |user| %>
        <div class='well cust-users'>
          <h2><%= link_to "#{user.username}", user %> </h2>
          <p> <%= pluralize user.photos.count, 'photo' %></p>
        </div>
      <% end %>
    </div>
  </div>
</div>
