<section>
  <% photos.each do |photo| %>

  <% if photo.image.thumb_200.url.nil? %>
    <%= link_to '???', photo %>
  <% else %>
    <%= link_to (image_tag photo.image.thumb_200.url), photo %>
  <% end %>

  <% end %>
</section>
